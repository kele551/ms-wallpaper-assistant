# 更新日志

本项目采用语义化版本：`MAJOR.MINOR.PATCH`。每次改版打一个 git tag，并在 Gitee 上写一版发行版（含可直接下载的压缩包）。

> **哪些版本真正对外发过**：当前对外发布版本是 **v2.0.7**（2026-09-22，Gitee 与 GitHub 同步，
> 已打 tag `v2.0.7`、已建发行版）。
> `v2.0.3` ~ `v2.0.6` 是**本机迭代版本**（修缺陷、改界面），**没有单独发版**；
> 它们的改动全部包含在 **v2.0.7** 里，已随 v2.0.7 一并对外发布。
> **图标试版不占版本号**：中途试过两版新图标（**没有版本号、没有打包发布**），
> 作者看过实机效果后决定**换回原来那张**，这两次试版**已全部删除：不发版、不打 tag、不留档**，
> 所以版本号仍然停在改图标前的 **v2.0.7**。
> （下面的条目按本机开发顺序保留，便于对照，不代表每个版本都有对外下载。）

## v2.0.7 — 2026-09-22

> 作者要求：**菜单再优化一下**。只动显示与文案，不动任何功能。
> 图标方面：试过两版新图标后，作者看过实机效果，结论是**原图标最好看** ——
> 本版把图标保持/恢复为原来那张（与原文件逐字节一致），**以后不再改图标**。
> 试过的版本与对比图都留在工作区的 `logs\图标候选-壁纸助手\`，以后想看还能看。

- **主菜单重排**：原来首页堆了十来行状态与提示、一屏挤满，现在压成**三行**——
  `换图`（下次自动换/间隔/后台是否在跑/今日必应）、`图库`（必应/聚焦/待换队列/累计下载）、
  `位置` 与 `壁纸`（当前这张），另外只在开着「只看收藏」时才多一行。
- **提醒改成「有问题才出现」**：坏图、超上限、聚焦库空、外来图、有新版本、位置不安全、
  库空但换过图、程序位置变过 —— 全部收进一个黄色提醒区，一切正常时一行都不显示。
- **按键分组**：`换图`（[1][2][3][4][F]）、`后台与设置`（[A]/[B]/[S]/[U]）、`其它`（[L][R][Q]），
  每组一条分隔线，不再是一长串混排。
- **[A]/[B] 文案说清**：「后台这次有没有在跑」和「下次开机起不起」是两件事，
  现在分成 `开机自动换: 开 · 后台正在跑` / `开机自动换: 开 · 后台没在跑（按 [A] 拉起来）` 两种状态写。
- **[U] 检查更新常驻**在「后台与设置」里，想手动查随时能按。
- 颜色：状态灰、提醒黄、可操作键保持默认、当前壁纸用白。

## v2.0.6 — 2026-09-22

> 作者要求：**升级界面要好看、动态，显示从哪个版本升到哪个版本**。

- **新：在线升级界面**：标题框 + **版本迁移动画**（`v2.0.6 ====> v2.0.7` 箭头逐格流动）+
  三步进度（连接升级源 / 下载 / 校验与安装）+ 下载时游标持续流动 + 绿框「升级完成 -> vX」。
- **新：后台静默升级后，下次打开菜单提示一行**「已自动升级到 vX（本次运行生效）」。
- 界面只用 ASCII 画（部分控制台字体画不出方块字符），靠颜色与动效撑场面。
- **修**：`menu.ps1` 的版本号常量之前漏跟（2.0.4 一直没改成 2.0.5），这次对齐到 2.0.6。

## v2.0.4 — 2026-09-22

> 作者要求：**程序要带本人署名**，文件名不带版本号（鼠标悬停看属性里的版本）。

- **加作者署名**：exe 文件属性（公司/版权/备注）、主菜单与首次运行界面、源码文件头、
  README 与使用说明，统一为「海风（kele551）」（属性里用 ASCII 版，避免乱码）。
  原来的版权行写的是 `MIT License`（把许可证名当版权了），已改正。
- **使用说明.txt 去掉写死的版本号**（原来还停在 v2.0.0），版本统一看 exe 属性。
- 协作记录（Cindy）只保留在源码注释里，不出现在界面上。

## v2.0.3 — 2026-09-22

> **起因**：作者表弟发来一张截图 —— 程序把图库建在了 `E:\V9筑龙资料云南版\图片\壁纸`，一个**资料目录**里。
> 作者的要求：「应该自动判断哪些地方可以放，哪些地方不能乱放。」这一版就是修这个。

- **修：程序会把图库安顿进别人的资料/工作目录。** 旧的自动定位规则里有一条「深度 2 扫描」：在非系统盘的**二级目录**里找名叫「图片 / 我的图片 / Pictures / 照片 / Images / 图库」的文件夹，**只要里面有一张 `.jpg`** 就认定它是本机的图片文件夹，随即在它下面建「壁纸」。别人的资料盘、工程目录里本来就可能存着几张图，于是 `E:\V9筑龙资料云南版\图片` 中招 —— 那个目录里有一张图，程序就把库搬进了他的工作资料里。现在只有**已经存在本程序自己的库结构**（`壁纸\必应` 或 `壁纸\聚焦` 里有图）才复用，光有几张散图一律不再采纳。
- **新：加了一道「这地方该不该放」的判断。** 沿着候选位置往上追：任何一层目录名带「资料 / 教程 / 课程 / 课件 / 素材 / 项目 / 工程 / 源码 / 备份 / 存档 / 培训 / 标书 / 图纸」这类词，或者「图片」文件夹所在的那一层混着文档 / 表格 / 安装包 / 程序文件（不是图片的东西）→ **直接排除**，绝不往里写。这条只约束程序**自动挑位置**；用户在首次运行向导或设置里手动指定的路径不受限制（那是他自己的决定）。
- **新：已经在错地方的机器有提示 + 一键搬家。** 主菜单发现当前保存位置不该放时，会在「保存位置」下面显示一行黄色提示并指明怎么改；用户按 `[S]` → `[3]` 换位置后，程序会问「要把已下载的图一起搬过去吗」。搬家只搬**程序自己下载的图片**，别的文件一个字都不动；源目录里只要还留着用户的文件，那个目录就不删（用非递归删除当保险丝，删不掉说明里面还有东西）；程序自己建的「壁纸」那层也只在空了之后才删。
- **不变（重要）**：覆盖安装**不会**改变已有的保存位置，也**不会**自动移动任何图片 —— 位置记在 `config.json` 里，升级只替换程序脚本；搬家只在用户主动操作时发生。

## v2.0.2 — 2026-09-22

> **为什么同一天出两版**：v2.0.1 上午发布后，作者委托做了一次完整代码审计（逐行读完 3200 余行，并用发布出去的同一份代码在沙箱复现验证），发现一个会导致「壁纸再也不自动换」的严重缺陷，当天中午即修复补发 v2.0.2。**两版之间没有任何功能增减：v2.0.2 = v2.0.1 + 本次缺陷修复。**

一次完整的代码审计后出的修复版：把 v2.0.1 的 `core.ps1` / `menu.ps1`（合计 3200 余行）逐行读了一遍，并用真实脚本在沙箱里复现验证，修掉 1 个严重缺陷 + 3 类中等缺陷。

- **修（严重）：升级时脚本无限递归、最终栈溢出，而且升级结果永远写不进硬盘。** `Get-BwState` 在把 `state.json` 从 schema 6 升到 7 时调用 `Save-BwStateKeepFav` 落盘，而后者**第一件事**就是再读一次 state（`Get-BwState`）——磁盘此刻还是旧 schema，于是又进同一分支、再调、无限递归，直到撞上 PowerShell 的调用深度上限抛 `The script failed due to call depth overflow`。沙箱实测（就用发布出去的这份 core.ps1）：20~35 秒刷约 595 行重复日志后抛异常，磁盘 `state.json` 始终停在 schema 6、回填标记永远为空——**升级结果从未落盘**。真实日志里 2026-09-20 15:52 就是这个现场（595 条 `节奏进度文件 6 -> 7`）。又因为 `Invoke-BwCycle` 把 `Get-BwState` 包在 try 里，异常被吞成一句「轮换异常」，用户看到的现象是**壁纸再也不自动换**。影响范围：所有从 v1.6.2 及更早版本升级上来的用户。现在迁移落盘改走**不重入的直写**（这份 state 就是刚读出来的，只改了 schema 和 dl_filled，不需要合并），实测 0.15 秒完成、schema 与 `dl_filled` 正常写入。
- **修：用户自己放进图库的图会被当成坏图挪走。** `Test-BwImageOk` 的三层校验（文件 ≥100KB + 头 `FF D8 FF` + 尾 `FF D9`）是给「程序自己下载的 jpg」定的，但它同时被用在「手动挑图设壁纸」和「图库巡检」上：用户丢进图库的 png / webp / bmp / gif / tif 头几个字节不是 `FF D8 FF`，一律被判「已损坏」并移进「坏图」文件夹；不到 100KB 的小照片同理。实测一张有效 PNG 的头 3 字节是 `89 50 4E`，必然判假。现在按来源分流：**程序下载的图走严格校验**（截断必抓），**用户自己的图只要求「能打开」**（新增 `Test-BwUsableImage`）。巡检测试里旧代码把「用户的图」和「截断的下载图」两张都挪走，新代码只挪后者。
- **修：改「换图间隔」时填长数字会把菜单搞崩 —— 而数值护栏正是为防这类事写的。** `[S] → [1] 换图间隔` 里填 11 位以上数字（如 `99999999999`）时，`[int]$mm` 自己就抛 Int32 溢出异常，而护栏 `Limit-BwNum` 是在转换**之后**才生效的——护栏还没轮上，菜单先被干掉（该异常不在任何 try 里，直接冒到主循环终止菜单）。`[S]` 里选保存位置那处同理。现在统一改用 `[int]::TryParse`；位数超出 Int32 就按「比上限还大」处理，交给护栏夹回 1440 —— 仍然符合原设计「越界夹回边界，不报错、也不把用户打回去重填」。
- **修：后台换图期间，你刚做的收藏/进度会被无声抹掉。** `Save-BwState` 是整份覆盖写；而补漏最多连着下 30 天、要好几分钟，这期间用户在菜单里按 [F] 收藏、手动挑图，写的是同一份文件，后台跑完一覆盖就没了（不报错、不写日志，用户只觉得"这软件有毛病"）。`Invoke-BwCycle` 的注释里其实已经承认过这类漏改并修了三处，但 `Invoke-BwBackfill`、以及菜单的 `Set-BwBase` / `Invoke-BwManualSwap` / `Invoke-BwRefill` / 收藏夹开关 / 首次运行向导共 **6 处**仍在用整份覆盖。现在统一走 `Save-BwStateKeepFav` 合并写（收藏以磁盘为准、看过名单取并集、当前壁纸取较新）。**刚改过收藏的那几处必须继续用整份写**（合并写以磁盘上的收藏为准，用上去等于把刚收藏的抹掉），代码里已加注释说明，免得以后被"顺手统一"改坏。
- 版本号 2.0.1 → 2.0.2（升号是为了让已装用户覆盖 exe 后自动重新释放新脚本）。

## v2.0.1 — 2026-09-22

一处性能修复（仅此一项，故升 PATCH 号）：

- **修：打开菜单要等约 5 秒内容才出现。** 根因：进菜单前同步跑的坏图扫描 `Sweep-BwBadImages` 会遍历两个图库的所有 jpg、逐张用 GDI+ 完整解码来验伤，耗时和图库张数**严格成正比**——图越多越慢、且会随图库增长一直恶化（这正是 2.0 才出现、旧版没有的原因：坏图隔离是后来加的功能）。现在把扫描改成**后台作业**：菜单立刻显示，扫描在后台跑完后再提示「已隔离 N 张」。坏图隔离功能本身完全保留，只是挪到后台，不再阻塞菜单。本机图库仅 54 张、扫描不到 1 秒，无法在此实测出 5 秒；但逐张解码的机制已确定消除，菜单恒为秒开。

## v2.0.0 — 2026-09-21（大版本）

这一版攒的变动太大，升主版本号。核心是**让图库里只有能用的图**，外加下载时的进度看得见：

- **坏图自动隔离，绝不上桌面。** 新增 `Test-BwImageOk` 三层校验（文件 ≥100KB、头 `FF D8 FF`、尾 `FF D9` 一票否决）。GDI+ 对截断的 JPEG 太宽容——半张图它也能「解码成功」——所以改以文件尾必须是 `FF D9` 为准；实测一张被截断在 4MB 的羽扇豆.jpg（尾字节 `8C 9C`）能被正确拦下。下载落盘后先过这关再入队；进菜单时 `Sweep-BwBadImages` 还会扫两个库，把损坏/截断的图挪进数据目录的「坏图」文件夹，库里只留好图。**修了 `Move-BwBadImage` 的空文件名 bug**：原来用 `Split-Path -Leaf` 取文件名，传入的路径为空时取到空串，目标变成 `坏图\.225526` 这种怪名、隔离直接失败；改用 `.NET` 的 `[System.IO.Path]::GetFileName` 后正常。
- **元数据写进图片本身，不再拖侧车。** 以前每张图旁边都带一个 `.meta.json`（标题/版权/来源等），文件管理器里混着一片白纸图标，还违反「库里只该有图」。现在元数据写入 JPEG 的**注释段（`FF FE`）**——`Write-BwImageMeta` 在文件尾 `FF D9` 前插入，不重编码、不影响看图，+191 字节、中文回读正确；`Read-BwImageMeta` 反向读出。`Sweep-BwBadImages` 顺手把库里散落的 `.meta.json` / `.part` 清走。现在壁纸库里**只有图片**。
- **`[2] 下载聚焦图片` 加了计数动效（方案 A：虚线增长 + 游标流动）。** 联网那几秒屏幕上不再一片死寂像卡死：固定的中括号轨道里，已下载的张数越多实线 `-` 越长（增长），一颗 `>` 游标在轨道里从左向右不停流动（流动），数字同步递增；两次下载之间的等待里游标也持续滚动几步。
- **收口说明：** v2.0.0 一并包含了 1.6.x 开发线里的全部改动——主菜单压到 4 个数字键（换图 / 下载聚焦 / 打开壁纸库 / 下载必应，必应两个入口并成一个二级菜单）、换图间隔改完最多 30 秒生效、关着不再显示假的「下次自动换」时刻（一律 `--:--`）、给允许手填的数字加上边界（间隔 / 每轮张数 / 图库上限）、去重认下载流水账不再重复下旧图、新增 `[R] 刷新画面`、同一字母不再跨级干两件事（收藏夹移出改 `x+序号`）。详见下方 v1.6.7 条目。

## v1.6.7 — 2026-09-21

两处修复 + 一处菜单整理：

- **修：聚焦图被反复重复下载。** 以前 `Invoke-SpotlightFetch` 只在「看过」名单 + 文件是否存在这两个维度去重；库被清空 / 回收站清走过、或「看过」名单超 400 淘汰后，已经下过的图会被当成新的再次下载，桌面还会把"早就下过的图"又设一遍。现在去重集合同时认 **下载流水账 `dl_ledger.log`**（每张程序下载的图都永久登记了编号）——下过的图绝不再下，宁可这一轮少下几张、等微软出新的，也不把旧图当新的重复下；并删掉了原来那个"下载到一半故意放行重下"的 `allowSeen` 兜底逻辑。必应图本就不重复，此问题仅影响聚焦。
- **修：改了数据目录的脚本总被旧 payload 覆盖回去。** 根因：上一版打包清单 `PAYLOAD_FILES` 还列着 `刷新图标缓存.bat`，而那个 bat 已从数据目录删掉、且从未打进 exe；于是每次启动 `sync_payload` 都判定"缺文件"，把内嵌的旧版 `core.ps1` / `menu.ps1` / `使用说明.txt` 全量重新释放，把你改过的东西打回原形。现在 `PAYLOAD_FILES` 不再列该 bat；新 exe 首次启动走"版本变更"分支，会按旧 `.files.json` 把残留的 `刷新图标缓存.bat` 一并清掉，再推送正确的脚本，重置循环从根上断掉。
- **菜单整理**：设置里去掉 `刷新图标缓存`（`[8]`）选项（v1.5 起图标已是独立 .ico，旧图标残留不再存在，且该选项的 ie4uinit 在不少机器上跑不起来）；腾出的 `[8]` 由 `图库上限` 顶上（原 `[9]` 上移一位，编号恢复连续）。打包清单与两份说明文档同步去掉对该 bat 的引用。
- **修：在设置 `[1] 换图间隔` 里改完分钟数，不会立刻见效。** 后台 `mode_daemon` 只在「到点 / 收到停止信号 / 有人换过图」三种情况下醒来，`cycle_minutes` 改没改它不看；于是把 30 分钟改小成 5 分钟后，还得按旧的 30 分钟睡满这一觉才轮到下一次判断，用户的体感就是"改了半天一点反应都没有"（反过来改大，则要白跑一轮才纠正过来）。现在内层的分片睡眠里加了一道"间隔变了就回外层重算目标时刻"，改完最多 30 秒生效，不用重启后台。间隔本身默认仍是 30 分钟。
- **菜单上那些数字不会自己变 —— 现在给个刷新键并写明。** 首页的「下次自动换」时刻、库里几张、队列剩几张，都是**进菜单那一刻算出来的快照**；窗口一直开着它就不会动，于是出现"时间一直停在 18:27、可图明明每 30 分钟在换"的困惑（那 18:27 是按当时 `last_swap` 17:57 + 间隔算的，之后后台照常换图，只是屏幕上那行没重画）。现在新增 **`[R] 刷新画面`**（重画一遍菜单，实测按一次重画一次），并把这话直接写在菜单项后面："上面那些数字是打开菜单那一刻算的，窗口一直开着不会自己变"；后台在跑时那行也补了"想看最新的按 [R]"。
- **关着的时候不再显示一个假的"下次自动换"时刻。** 那个点是按「上次换图时刻 + 间隔」算出来的，只要后台没在跑就永远兑现不了，而且数字纹丝不动 —— 显示它等于给个假承诺（海风："菜单里所有都是关的，时间要变成零"）。现在**只有后台进程真在跑**才给真实时刻；否则一律显示 `--:--`，并把状态与下一步写在后面：关着 →「自动换是关的，它不会自己换图 —— 想让它换，按 [A]」；开关开着但后台没跑 →「按 [A] 让它现在就开始」。不用 `00:00` 是因为那会被读成"半夜十二点换一张"，反而更误导。
- **换图间隔不再是"隐藏功能"（只加提示，不挪位置）。** 它以前只在设置页里以编号 `[1]` 露面，主菜单上连「换图间隔」四个字都没有 —— 不知道有这么个"1"可以按，这功能等于不存在（用户原话："你不说明，谁会去按 1"）。改法不是把它搬到主菜单（试过在「换图」组加 `[I]`，结果一级、二级菜单各有一处，反而更乱、也更突兀，已撤回），而是**在设置页那一行后面直接把操作写明**：`[1] 换图间隔   每 30 分钟换一张   按 1 就能改`（后半句黄字），并给整个设置页补一句通用说明"想改哪一项，就输入它前面的数字再回车"。首页状态行顺带把当前值写出来（「下次自动换: 15:52（每 30 分钟一张，后台正在跑）」），不进设置也能看见现在是几分钟一张。改间隔的逻辑抽成 `Edit-BwCycleMinutes`，设置 `[1]` 走它。实测模拟按键 `S → 1 → 60 → 回车`，配置从 30 变成 60。
- **同一个字母 R 在两级菜单里干两件不同的事 —— 改掉。** 主菜单 `[R]` 是「刷新画面」，而收藏夹里 `r+数字` 是「移出收藏」（如 `r3`）。一个键两个含义，按下去之前得先想清楚自己在哪一级，这是典型的自己给自己挖坑。现在收藏夹改用 **`x+数字`**（x 像"叉掉"，直观），主菜单的 `[R]` 保持刷新不变；按了旧的 `r` 会给一句提示"移出收藏现在按 x+序号 —— r 在主菜单里是「刷新」"，不至于按了没反应还不知道为什么。
- **菜单瘦身：砍掉口水话，只留状态和操作。** 之前每行都要跟一句解释性括注（「[1] 立刻换一张 (聚焦)」「[7] 补下最近错过的必应 (几天没开机)」「[R] 刷新画面（上面那些数字是打开菜单那一刻算的，窗口一直开着不会自己变）」「累计下载: 33 张 (从装上那天算起, 删掉和清走的都记着)」…），一屏塞了 30 多行，真正要看的数字反而被淹了。现在统一收成短句：`[1] 立刻换一张`、`[7] 补下错过的必应`、`[R] 刷新`、`累计下载: 33 张`；状态行同理（`下次自动换: --:--  (自动换关着 —— 按 [A] 开)`）；原来三行的「库里一张图都没有」警告并成一行。主菜单从 30 多行压到 **24 行**（含标题与分组）。`使用说明.txt` 的菜单清单同步对齐。
- **清掉死代码** `tests/refresh_icon_cache.py`：图标缓存功能连同设置里那个 `[8]` 选项一起删掉之后，这个脚本在仓库里已无任何引用（Grep 全仓零命中），留着只会让后来人以为它还接在哪条路上。
- **主菜单压到 4 个数字键，两个必应入口合成一个。** 原来一级菜单挂着 8 个编号，其中 `[2] 换上今天的必应每日一图`、`[5] 随便来一张`、`[7] 补下错过的必应`、`[8] 下载往期必应` 四条里有三条各自只干一件小事，还都跟"必应"有关。现在一级菜单只剩 **1 换图 / 2 下载聚焦图片 / 3 打开壁纸库 / 4 下载必应图片**（字母键不变：`[F]` 收藏、`[S]` 设置、`[A]`/`[B]` 自动换、`[L]` 日志、`[R]` 刷新、`[Q]` 退出）：
  - `[1] 换图` —— 就是原来的「立刻换一张」，从聚焦库按队列顺序换下一张；
  - `[2] 下载聚焦图片` —— 原来的「再抓一批新聚焦」；
  - `[3] 打开壁纸库` —— 原来的「从库里挑一张」，进去能按序号设壁纸、`f+序号` 收藏、`o` 打开文件夹；原来那个只开资源管理器窗口的「打开壁纸文件夹」不再单独占一个号，改由这里面的 `o` 承担；
  - `[4] 下载必应图片` —— 二级菜单：`[1] 补齐必应图片`（按年-月整月下载，范围 2021-02 至今）、`[2] 补漏必应图片`（原来那个 `[7]`，补下载错过的那些天）。
  - **去掉 `[5] 随便来一张`**：它和 `[1]` 看着像一回事 —— 实测才知道 `[1]` 走聚焦队列、换完出队、一轮内不重样；`[5]` 是从必应 + 聚焦两个库里纯随机抽、可能抽到刚看过的、且完全不动队列。差别说不清，又和「挑一张」重叠。随它一起删掉 `core.ps1` 里已无调用者的 `Invoke-BwRandom`（全仓 Grep 零命中）。
  - 编号不留空号（与当初设置页 `[9]` 上顶 `[8]` 的规矩一致）。`使用说明.txt` / `README.md` 的菜单清单同步重写；顺带修正说明里收藏夹还写着的 `r3`（移出收藏早已改成 `x+序号`，`r` 在主菜单是刷新）。
- **去掉「换图」这个分组标题。** 重排之后它底下只剩 `[1] 换图` 一项 —— 一个标题罩着一个孤零零的选项，看着像还有别的，白占一行。现在 `[1] 换图` 直接顶在菜单最上面，下面才是「图库」组。
- **给允许用户自己填的数字加上边界**（换图间隔 / 每轮张数 / 图库上限）。这三个以前只校验"是正整数"，等于乱填也放行：换图间隔填 `1` 就是每分钟换一张；填到约 41.9 亿分钟以上（≈7978 年）时，后台算"下次几点换"的 `AddMinutes` 会越过 DateTime 上限（9999-12-31）抛异常 —— 实测 4193538076 分钟是能加的极限、4194000000 就崩，守护进程当场摔死，表现就是"壁纸再也不换了"；`每轮抓几张` 填 9999 会让一轮补图真去抓 9999 张，跑到像卡死。现在在 `core.ps1` 里建了一张唯一的范围表 `$global:BwLimit`：**换图间隔 5~1440 分钟（默认仍是 30，不设档、照样自己填）**、每轮 1~50 张（默认 6）、图库上限 0=不限 或 5~2000 张（默认 100）。越界的数**不报错也不打回去重填**，自动贴到最近的边界并告诉你取了多少；负数或填不出数的退回该项默认值。防护分两层：`Get-BwConfig` 读出来就夹（菜单显示、后台执行、首页"下次几点换"共用同一口径，不会各说各话）；进设置时 `Repair-BwConfig` 再拿**磁盘原始值**比对并落盘 —— 第一版是拿已夹过的内存值跟自己比，永远没差异、落盘净化形同虚设，是被实测揪出来的。手改 `config.json` 写进去的脏值会被纠正并给一行黄色提醒；后台守护（Python `read_interval`）加了同一套夹取。

## v1.6 — 2026-09-20

把"照着提示做却做不到"和"按错一个键就关掉程序"这类手感问题修掉；顺便把用户想看的那个数字摆到首页。

- **v1.6.6** 一轮全量代码复核后的修补，十处：**① 外来图改成「只做记号、不动文件」** —— v1.6.4 的做法是把不在下载清单里的图直接移进「_外来待确认」，那是程序替用户挪文件、还不打招呼；现在只把编号记进 state（图原地不动、不改名、不删），自动轮换时跳过它们，菜单首页报个数、`[4]` 列表里标「外」，全程不用你管。**② 菜单 `[2]` 换必应不再被后台抢回去** —— 以前手动切必应只换壁纸不记账，15 分钟后后台看到"今天还没切必应"又插一张，把你刚挑的图顶掉；现在手动切也记账。**③ 后台规则 2/3 写回改走 KeepFav** —— 规则 1 早就这么写了，重启换图和半小时换图这两条却还在整份覆盖，换图跑的那几分钟里你在菜单里的收藏/挑图会被无声吞掉。**④ 补漏分批** —— 以前一口气列出整段缺口，半年没开机就是 180 张 4K，一次巡检十几分钟被时限掐断、水位线还推不动，于是每次从头补、永远补不完；现在一轮最多补 30 天，水位线照样往前推。**⑤ `-DryRun` 名副其实** —— 少了 Save-BwFile 那道闸，试运行时后台补漏照样真下载；现在试运行一张都不下。**⑥ 小修**：菜单 `[3]` 抓来的新图不再被当成"已删除"剔出队列（队列存文件名，却塞了完整路径进去）、随机换一张也记账、`Get-BwCandidates` 去掉 `,$urls` 那个会把数组压成一个元素的写法、查重路径统一用 `-LiteralPath`（库路径带 `[]` 时不再查不出来）、空库时不再每显示一次"累计下载"就重扫一遍两个库。**⑦ 不认 jpg 以外的图片** —— 图库扫描全线写死 `-Filter *.jpg`，往库里丢一张 webp/png 的照片，程序完全看不见：不标记、不提示、也没人管（实测丢 `IMG_2782.webp` 毫无反应）。现在统一到一份图片格式口径（jpg/jpeg/png/webp/bmp/gif/tif/tiff）：认外来图、菜单 `[4]` 列表都按它走，这类图会被标记、列表里标「外」，全程不动文件。**⑧ 库目录被删了自己建回来** —— 进菜单和每次巡检都先把两个库目录立起来，不用你手动去新建一个空文件夹；聚焦库空了（目录刚重建）就自动在后台补一批图，必应库只把目录建回来、历史图由你自己用 `[7]` 补漏 / `[8]` 归档恢复，不打乱你的东西。**⑨ 菜单字体大一号** —— 控制台字高 16px 提到 20px，中文长标题不再挤成一团；设置失败就保持原样，不影响功能。**⑩ 清掉死代码** `Show-Browse`（被 `Show-BrowseAll` 取代后已无调用者），并改正 `[9]` 处还写着 v1.6.4 旧行为的注释
- **v1.6.5** 换图不再让用户干等下载。以前队列空了要**整批下满**（默认 6 张，每张之间还停 0.4~0.6 秒）才肯换壁纸，手动按 `[1]` 得盯着屏幕等 20 多秒。现在分两层：**① 换完一张就预判** —— 队列剩 2 张或更少时，立刻起一个隐藏的后台进程把货补满（下完顺手重洗队列，新图马上进轮换），等你下次换图时图早就在库里，前台零等待（实测：换图 0 秒，后台同时把 6 张补齐）；**② 真到队列空了**（刚装好、或全看过）也只**先下 1 张**就回来换壁纸，剩余张数照样丢后台慢慢补。补货进程带锁（15 分钟自动过期），菜单和后台同时触发不会各起一个。**另修一个"清空库就不再补图"的老毛病**：把聚焦库清空后，以前会卡在"按设置不下载新图、把库里现有的 0 张重洗"，只能手动按 `[3]` 才有图可换 —— 现在把"库空"当成异常状态，不论 `auto_fetch` 开关是否打开都自动在后台补一批，前台先下 1 张立刻换上，不空窗也不劳用户动手。
- **v1.6.4** 把 v1.6.3 临时加的「[9] 校验图库」手动按钮去掉, 校验改由程序**在后台每次巡检时自动、静默完成**: 扫描两个壁纸库, 凡是图片编号不在下载清单 (dl_ledger.log) 里的, 直接移进各自库的「_外来待确认」文件夹 (不删除, 随时能搬回), 只在日志留一行「自动隔离外来图」。这活本就是程序的责任, 不该让用户去按一个键; 同样, 累计下载那个数字也只是程序自己统计维护、供排查用的, 用户无需理会。这也回应了"混入别人的照片怎么分辨" —— 现在不用你管, 程序自己分。
- **v1.6.3** 修「累计下载」越用越小的 bug：v1.6.1 把这个数记在 state.json 里，但菜单进程一直握着自己那份旧快照，之后任何一次整份写回都会把下载时刚 +1 的数打回旧值 —— 实测一天连下 6 张，6 次 +1 全被吞，累计显示 53、库里却有 59 张，账根本对不上。现在改记到**独立的流水文件**（每下载一张追加一行图片编号，追加写不会被任何进程覆盖，state 里只留显示缓存），谁覆盖都动不了真账；数字自动按痕迹修正回能追溯到的准确值。同一份流水顺带成了「程序下载清单」：新增菜单 `[9] 校验图库`，编号在清单里的 = 程序下载的，库里出现清单外的图（手动放进来的、改过名的）一眼就能列出来；要不要挪到「待检查」文件夹由你点头，程序默认只报告不动文件
- **v1.6.2** 壁纸存哪**自己找现成的**，不再凭空造空文件夹：C 盘以外只要已经有放图片的文件夹（「图片」「我的图片」「Pictures」等），就直接定位到它下面的「壁纸」；盘根已有的「壁纸」库也算数。以前是挑一个非系统盘然后建 `<盘>\图片\壁纸`，于是明明盘上就有「我的图片」，盘根还是多出一个空的「图片」。设置 `[3]` 改位置时终于带上建议项（以前传的是空建议，列表里没有「<-- 建议」标记，用户只能自己猜着挑，挑错就多一个空壳）。顺带把"后台还没启动"这类措辞改掉：菜单开着 ≠ 后台在跑，现在如实说「后台这会儿没在跑」；并且开机自动换开着、后台却没跑时，菜单会同时给出 `[A]` 把后台拉起来和 `[B]` 整个关掉（以前只摆 `[B]`，首页却又写"按 `[A]` 开"，用户照着按 A 只能靠猜）。**另修一个老毛病**：第一次按 `[A]` 开自动换之后，一关菜单窗口就弹「Failed to remove temporary directory」—— 根因是菜单 exe 经 PowerShell 拉起后台时，PyInstaller 留在环境变量里的 `_PYI_*`/`_MEIPASS2` 标记被一路继承，后台 exe 误以为自己是菜单的子进程，直接复用菜单的 Temp 临时目录而不自建；菜单退出要回收目录时后台还占着，删不掉就弹框。现在启动 PowerShell 子进程前把这些标记剥干净，后台各用各的临时目录，关窗不再弹框（这个毛病"每个新版本第一次使用才出现"，也正好对上：只有第一次才需要按 `[A]`）
- **v1.6.1** 菜单首页新增「累计下载：N 张」：从装上那天算起一共下载过多少张壁纸，**删掉的、被库上限清进回收站的都还在这个数里**（它是笔只增不减的流水账，不会因为整理图库就往回缩）。老版本升级上来会自动补一个基数——按「库里现存的 + 「看过」名单 + 日志里的下载记录」三处按图片编号去重取并集；日志超过 256KB 会只留最近 200 行，所以更早就下载过、痕迹已经找不回的补不回来（数字只会偏小，不会凭空变大）。从这版起每成功下载一张就真记一次（`Save-BwFile` 里统一 +1，走 `Save-BwStateKeepFav` 合并写回，后台和菜单同时下载也不会互相覆盖）
- **v1.6.0** 菜单设置的 `[8] 刷新图标缓存` 在系统自带命令失灵时，给出的备用脚本是**真能找到的那个**（以前提示去"程序目录"找 `刷新图标缓存.bat`，而那个 bat 压根没打进 exe，用户照做只能扑空；现在 bat 跟着程序一起落到数据目录，提示里直接给完整路径，还能按 `[o]` 打开它所在的文件夹）；主菜单按 `o` **不再退出程序**（设置里 `[o]` 是「打开文件夹」，同一个键在主菜单却直接关掉程序，太容易误伤 —— 现在会提示退出请按 `Q`）；修掉两处指错路的提示（首运行说"设置 `[1]` 改保存位置"实际是 `[3]`；说"再按一次 `[A]` 就关掉"实际是 `[B]`）；文档补齐设置里的 `[8] 图标缓存`，并改正卸载步骤里把 `[A]`/`[B]` 写反的错

## v1.5 — 2026-09-19

双击就能用：只出一份 exe、数据搬出程序目录、开机自动换不会再按反、看过的壁纸不再排回来、图库不再越攒越大。

- **v1.5.5** 修掉几处"悄悄出错还看不出来"的问题：下载到的半成品/错误页不再留在图库里（更不会被设成壁纸）；补漏某天没联网时不再把那天永久判死，下次会重试；当日必应图没拿到时不假装成功、真的 15 分钟后再试；后台巡检不再无声覆盖你在菜单里的收藏和手动挑的图；手动挑图的路子终于记账了（`last_wall`／看过名单都写），按 `[F]` 收藏到的就是眼前这张
- **v1.5.4** 图库设上限（默认 100 张），超了把**已经看过**的最老的移进回收站；新增「自动补新图」开关（默认关，只在现有图里轮换）；老版本升级时从日志回填「看过」名单
- **v1.5.3** 看过的壁纸不再排回来（另记一份「看过」名单，按图片编号比对而不是文件名）；按 `[A]` 开自动换**当场**就生效，不用等下次开机
- **v1.5.2** 「开机自动换」拆成 `[A]` 打开 / `[B]` 关掉两个键，不会再按反
- **v1.5.1** 只出一份 exe，不用挑该点哪个；第一次打开不再追问壁纸存哪
- **v1.5.0** 配置和收藏搬到用户目录，exe 旁边不再多出任何文件夹

## v1.4.2 — 2026-09-19

**修：快捷方式被删后可能又在桌面根冒出来**

- v1.4.1 在"桌面上找到了用户挪走的快捷方式"这条路径上，只记了位置 `path`，
  **没把"已经建过"的记号 `created` 补上**。实测 `desktop_shortcut.json` 长这样：
  `{"path":"F:\\桌面\\图标\\微软壁纸助手.lnk","created":""}`。
- 后果：这个快捷方式一旦被删（或被挪出桌面的一层子目录），程序会按"从来没建过"
  处理 → 又在桌面根目录新建一个 —— 正是 v1.4.1 要解决的那个毛病。
- 现在两条路径都补齐 `created='yes'`：
  记着的位置还在 → 顺手补记号（老文件也能自愈）；桌面里找到 → 找到即算建过。
- 手上那份 v1.4.1 不用急着换：把 `desktop_shortcut.json` 里的 `created` 改成 `"yes"` 就等效。

## v1.4.1 — 2026-09-19

**桌面快捷方式：只自动建一次，之后跟着你放的位置走**

- v1.4.0 的做法是每次进菜单都核对 `桌面\微软壁纸助手.lnk`，不在就建。
  于是**你把快捷方式收进桌面上的文件夹（比如「图标」）以后，桌面根目录又会冒出一个新的** ——
  每打开一次菜单冒一个，等于跟用户的桌面习惯较劲。
- 现在程序把快捷方式的位置记在 `desktop_shortcut.json` 里：
  - 记着的位置还在 → 就地更新（收进文件夹也跟着，不在桌面根另建）；
  - 记的位置没了 → 先在桌面根和一层子目录里找，找到了就接着用那个；
  - 找不着且从来没建过 → 建**这一次**；
  - 建过又被删掉 → 返回 `gone`，**不再自动建**（尊重你删掉它的决定）。
- 想重建：`[S] 设置 → [6]` 关一次再开。
- 状态单独存 `desktop_shortcut.json`，**不写进 `config.json`** ——
  主菜单靠"config.json 在不在"判断要不要走首次运行向导，要是建快捷方式时顺手把
  config.json 也写了，用户还没选保存目录，向导就被跳过，壁纸会默默存到默认位置。
- 快捷方式的悬停提示带上版本号（`微软壁纸助手 v1.4.1 - 双击打开菜单`），
  鼠标停在上面就能看出是哪一版。

## v1.4.0 — 2026-09-19

**壁纸填充方式可以自己选了（以前是写死的）**

- 以前 `Set-BwDesktopWallpaper` 每次换图都硬写 `WallpaperStyle=10`（填充），
  等于**反复覆盖用户在系统设置里选的填充方式** —— 你想用"适应"留个完整的画面，
  下次换图就被改回"填充"裁掉了。
- 新增配置项 `wallpaper_style`：填充 / 适应 / 拉伸 / 居中 / 平铺 / 跨区。
  默认是 `fill`（填充），与 v1.3.0 及更早的行为一致 —— 老用户升级后桌面不会变样。
- 只在**值与注册表当前值不同时**才写注册表，避免每次换图多做一次无谓的写入。
- `[S] 设置` 新增 `[4] 壁纸填充方式`，改完**立刻重设一次当前壁纸**，当场就能看到效果
  （`Apply-BwWallStyle`）。只改填充方式不换图的话，不然得等下一次轮换才看得出来。

**收藏**

- 看到一张喜欢的图，主菜单按 `[F]` 就收下；再按一次取消。
- `[4] 从库里挑一张` 的列表里，收藏过的标 ★，输入 `f+序号`（如 `f3`）收藏/取消。
- `[S] 设置` 新增 `[5] 收藏夹`：看收藏、设为壁纸、`r+序号` 移出，
  以及开关「**只在收藏里轮换**」（开了之后自动换图只从收藏里挑）。
- 名单存在 `state.json` 的 `favorites` 里，**不往壁纸目录写 sidecar 文件** ——
  那样用户整理图片时会看到一堆额外的小文件，把壁纸文件夹弄脏。
- 收藏的图被删掉后，名单里的名字**留着不清理**（拷回来收藏还在），只是取用时跳过；
  收藏夹里会如实标注"另有 N 张收藏的图已经不在库里了"。
- 收藏可以是必应库的图。队列原本只在聚焦目录找图，现在找不到会再去必应目录找一次，
  所以「只看收藏」模式下必应的图也能正常轮换。
- 「只看收藏」但收藏里一张能用的都没有时，**退回洗整个库**，不会卡在"挑不出图 → 永远不换壁纸"。

**桌面快捷方式：自动建、自动修，不用自己动手**

- 以前要自己往桌面拖一个快捷方式，换版本或挪位置后还得重建 —— 不然双击的是老版本，
  或者压根双击不开。
- 现在每次进菜单都核对一次桌面上的 `微软壁纸助手.lnk`：
  - 没有 → 建一个（**首次运行就会有**，向导最后会告诉你一句）；
  - exe 换过地方 → 改指到当前这份；
  - exe 被新版本覆盖过（文件比快捷方式新）→ 重存一次，顺带把桌面图标刷新成新版的。
- 不想要就在 `[S] 设置 → [6]` 里关掉，之后**不再自动建**，桌面那个也会删掉。
- 快捷方式指向固定名 `微软壁纸助手.exe`，与开机自启用同一份，换版本不会断。

**换了新图标**

- 旧图标是深色夜景加一层糊掉的倒影，缩到 48px 以下就看不清画的是什么。
- 新图标：圆角方块 + 晨色天空渐变 + 太阳 + 两层山，元素压到三样，
  16 / 32 / 48 / 64 / 128 / 256 六档尺寸逐档出图，小尺寸也认得出。
- 32px 和 16px 单独画（不带光晕），否则光晕一缩就糊成一片灰。

**修：菜单上「下次自动换」的时间到了却不换**

- 后台的节拍是"跑一轮 → 睡满 30 分钟 → 再跑一轮"，跟着**进程启动时刻**走；
  而菜单显示的时间、以及"该不该换"的判断，用的都是 `last_swap + 间隔`。
  两边不是同一个基准，一错位就出现"显示的时间过了，壁纸却没换"：
  - 手动按 `[1]` 换一张 → `last_swap` 前移 → 菜单显示的时间跟着前移，
    但后台还在按老节拍睡，醒来一算没到点，就又睡一整轮（最多拖 30 分钟）；
  - 重启后同理：必应图下载要花几分钟，`last_swap` 记的是**下载完成**的时刻，
    比后台启动时刻晚，于是下一拍又差一点没到点，白跑一轮。
- 改成**节拍跟着「上次换图时刻」走**：后台算出 `last_swap + 间隔` 这个目标时刻睡过去，
  并且每 30 秒看一眼 `state.json` —— 在菜单里手动换一张，后台马上重新对齐。
  现在菜单显示几点，就是几点换。

**修：exe 换个地方放，「开机自动换」会无声失效**

- `Test-BwAutoStart` 只看启动文件夹里那个快捷方式**文件在不在**，
  不看它指向的 exe 还在不在。exe 被挪走之后，菜单照样显示"开"，
  开机却什么都没拉起来 —— 不盯着看根本发现不了。
- 每次进菜单都核对一次快捷方式的目标，指错了就重新指到当前这份 exe
  （`Repair-BwAutoStart`），并在菜单上提示一行。exe 爱放哪个文件夹都行。

**修：换版本会让「开机自动换」断链**

- v1.3.0 的 CHANGELOG 写了"快捷方式指向固定名 `微软壁纸助手.exe`"，实际没落地 ——
  实测启动文件夹里那个快捷方式的目标是 `D:\Program Files\微软壁纸助手-v1.3.0.exe`，
  带版本号。出下一个版本时文件名一变，开机自动换就**无声失效**。
- `Get-BwLauncherPath` 改成：固定名存在就用它；不存在就**从当前 exe 复制一份出来**再指向它
  （只在 exe 自己所在目录里操作，不碰系统）；实在不行才退回当前 exe 路径，
  并且菜单里会提示"这份 exe 名字里带版本号, 以后换新版本要重开一次本开关"。

**修：`auto` 分辨率模式在高 DPI 屏上会挑错图**

- `[Screen]::PrimaryScreen.Bounds` 给的是**逻辑**像素。4K 屏开 150% 缩放时它只报 2560×1440，
  按 `Width -ge 3840` 判断就下成 1080p —— 图是缩水的，还看不出为什么。
- 新增 `Get-BwScreenPhysical`：乘上系统 DPI（`GetDpiForSystem`）还原成物理像素；
  拿不到 DPI 就按 96（100%）走，与旧行为一致。
- 现在配置是 `uhd`，本机不受影响；但凡有人用了 `auto` 就会踩。

**修：收藏第二张时名单会被拼坏（实测逮到）**

- `Add-BwFav` 原来写 `$s.favorites = @((Get-BwFav $s) + @($n))`。
  单元素数组从函数返回时会被**展开成标量字符串**，于是 `字符串 + 数组` 走的是
  **字符串拼接**而不是数组连接 —— 加第二张时两个文件名首尾相连变成一条，
  收藏夹里只剩一个拼坏的名字，两张都不生效。
- 实测证据：加第 2 张后 `favcount` 仍是 1，`Get-BwFavFiles` 返回 0。
- 改成两边都用 `@()` 包成数组。已加注释说明，免得后手又改回去。

**测试（均为实测，隔离目录，不动真实壁纸库）**

- state 迁移 4 → 5：schema=5、shown=25、水位线 2026-09-19、队列 2 项全部保留，收藏名单新建为空。
- 填充方式 6 档映射全对（含 TileWallpaper=1 只在平铺时出现）、非法值回退填充、大写 `FIT` 也认。
- 收藏：加入/去重/计数/命中/取文件（含必应库的图）/移出/移出不存在的返回 False，全通过；
  图被删后名单保留 3、取用时剔除到 2。
- 只看收藏：队列数=收藏数、含必应图、能取到图；收藏清空后退回整个库；关掉开关恢复洗整个库。
- 快捷方式：无固定名时自动复制出固定名并指向它；已有固定名则直接用；没有 launcher.txt 返回空。
- 未实测：`Apply-BwWallStyle` 实际改桌面的效果、`Get-BwScreenPhysical` 在本机的 DPI 取值 ——
  两者都依赖会改动系统的调用（改注册表 / 枚举显示器），留给升级后手动确认。

---

## v1.3.0 — 2026-09-18

**默认保存位置改成「图片」文件夹里的「壁纸」**

- 以前默认往盘根丢一个 `X:\微软壁纸助手`，跟系统图片库没关系。现在默认就是
  `<图片文件夹>\壁纸\必应` 和 `<图片文件夹>\壁纸\聚焦`，没有就自动建。
- 取的是系统「图片」文件夹的**真实位置**（.NET → 注册表 → 主目录下 Pictures 三档兜底），
  用户在资源管理器里把图片库挪到别的盘，程序跟着走，不写死 `C:\Users\xxx\Pictures`。
- **图片文件夹在系统盘时自动挪到系统盘以外**：壁纸每天都攒，几年下来好几个 GB，
  放在 C 盘容易把系统盘撑满。这时默认位置是 `<非系统盘>\图片\壁纸`，挑盘规则沿用
  `Get-BwPickRoot`（非系统盘优先、可写优先、可用空间降序）。只有系统盘的机器才用
  `<图片>\壁纸`，并且在向导里明说。
- 选择页第一项固定是系统图片文件夹，下面是各盘候选，仍然**只排序不筛选**，
  写不进去的盘照样能选、当场给一键修复。

**手动删掉 / 移走图片之后，程序照常工作**

用户会把图删掉、挪到别的文件夹、拷走 —— 这是正常操作，不是故障。之前有几处会被它带偏：

- **删掉最近几天的必应图，会被重新下载回来。** 补漏的起点是"库里最新日期"，
  那几张一删，起点就往回退，于是刚删的那些天又下一遍 —— 等于变相不许人删。
  现在 state 里记一条**水位线** `bing_high_date`，只往前不后退，
  它记的是"补漏办到哪天了"，不是"库里还剩几张"。实测：删掉 09-16/09-17 后
  库里最新日期退回 09-15，补漏起点仍是 09-17。
- **队列里留着一堆已经不存在的图。** 队列存的是文件名，图被删了就只能等轮到它
  才发现、一张一张慢慢淘汰。新增 `Sync-BwQueue`，取图前一次性剔除已不在库里的项。
  实测：5 张里删 2 张 → 剔除 2，剩 3，取图正常。
- **库目录整个不见了会误清队列。** 移动硬盘没插、网络盘没连上时读不到目录，
  这时候清队列等于把用户的轮换进度白丢。`Sync-BwQueue` 在这种情况下直接不动
  （实测剔除 0）。
- **"库里有几张"和"能轮换几张"对不上。** 计数走 `-Recurse`、队列不走，
  用户把图挪进子目录后就会显示 124 张却只换得动 100 张。现在一律只认库根目录
  这一层，两个数永远一致。
- **正在用的那张被移走了，菜单假装没看见。** 现在如实写"这张已经不在库里了"；
  整个库被清空时，会提示"图是不是被删掉或移到别处了"，并引导到设置里改位置。
- state schema 3 → 4：老进度（已换张数、队列、上次换图时间）**全部保留**，只多水位线字段。
  修掉了"schema 已存在就不会被改写，导致每次运行都重复走一遍升级分支"的问题。

**菜单重排 + 说法改直白**

- 按用途分组：换图（1-3）/ 壁纸库（4-6）/ 补图（7-8）/ 其他（S、A、L、Q），
  数字连号，不再 1 和 7 都是聚焦、2/3/4 都是必应地混着排。
- `[8] 设置` 不再是"一次问四个问题"，改成子菜单，当前值直接印在旁边，改哪项选哪项。
- 子菜单返回键统一 `[Q]`（原来归档页用 `[0]`、主菜单用 `[Q]`，两套）。
- 保存位置从"两个库各填一条完整路径"改成"填一个壁纸文件夹，下面自动分必应/聚焦"。
- 去掉「随机回忆」这种文艺说法，改「随便来一张」；状态行拆开摆，不再挤成一条。

**打包**

- 产物两份：`微软壁纸助手-v1.3.0.exe`（名字带版本号，好认）和
  `微软壁纸助手.exe`（固定名，部署用 —— 「开机自动换壁纸」的快捷方式指向它，
  换版本不断链）。
- 写进 Windows 版本资源，右键 - 属性 - 详细信息里能看到版本号。
  版本号以 `launcher.py` 的 `VERSION` 为唯一来源，`build-exe.py` 自动读取。

---

## v1.2.3 — 2026-09-18

**纠正 v1.2.2 的一个方向性错误：把"我这台机器的权限"当成了"产品的规则"**

- v1.2.2 检测本身没错，错在**展示**：探出某个盘根目录写不进去之后，把它**从编号列表里剔除**，
  单列成一行"用不了"。这等于拿一台机器的权限去替所有用户做决定 ——
  别人机器上那条 ACL 是齐的、盘明明能用，却在自己的列表里看不到它，只会以为"这程序不支持我的盘"。
- 现在**只排序、不筛选**：所有盘（含系统盘）都在编号列表里，一个都不少。
  当前写不进去的盘加一句说明 `这台机器上还没给写入权限, 选中可一键修`，**仍然可以选中**；
  系统盘排最后并标"不推荐"。默认值只是"排在最前面的那个"，它不剥夺任何人选别的盘的权利。
- 判据也跟着改了：`Test-BwRootWritable`（盘根可写吗）→ `Test-BwDriveUsable`
  （**本程序能不能在这个盘上用自己那个文件夹**）。盘根不可写、但 `<盘>\微软壁纸助手` 可写，
  也算能用 —— 否则用户修过一次之后，每次运行还会被再提示一遍"这个盘要修"。

**新增：盘权限一键修复 `Repair-BwBaseDir`**

- 用户选中一个当前写不进去的位置时，向导不再只回一句"这个位置写不进去"就静默退回默认位置，
  而是当场解释清楚 + 给两条路：`[1] 现在就修好它` / `[2] 我自己换一个位置`。
- 修复动作：用一次管理员权限建出 `<盘>\微软壁纸助手\必应` 和 `\聚焦`，
  并把"修改"权限**只**给 `<盘>\微软壁纸助手` 这一个文件夹
  （`Authenticated Users:(OI)(CI)(M)`，与 Windows 装机时给数据盘根目录写的那条一模一样）。
  **不动盘根，不动盘上任何别的目录**；把这个文件夹删掉就等于完全回退，不留任何权限改动。
  这也是它比 `icacls <盘>:\ /grant "Authenticated Users:(M)"`（改整个盘根）克制的地方。
- 只处理本地盘符；UNC 网络路径和更深的自定义路径不自动创建，如实告知用户自己先建好。

**修复：`New-Item -LiteralPath` 造成的假阴性不止一处**

- v1.2.2 只把"盘根探测"改成了 .NET，但 `Test-BwWritable`（写临时文件试可写）和 `Ensure-BwDirs`
  还在用 `New-Item -LiteralPath`。本机 PowerShell 5.1 的 `New-Item` **没有这个参数**，
  于是"给一个还不存在的目录测可写"必然抛异常返回 false —— 在向导里手输一个新路径会被一律判成写不进去。
- 新增 `New-BwDir`（纯 .NET `Directory.CreateDirectory`），
  `Ensure-BwDirs` / `Test-BwWritable` / `Repair-BwBaseDir` 统一走它，不再依赖任何 cmdlet 参数集。
- `[8] 设置` 里改库位置接上了同一套一键修复（新增 `Set-BwLibDir`）。

**测试（均为实测）**

- 盘根探测对照：`D:\` 根不可写，`E:\ F:\ C:\` 可写。
- 假阴性对照：`Test-BwWritable 'D:\_wbtmp'` = false；
  `Test-BwWritable 'F:\_wbtmp\a\b'`（目录**不存在**）= true —— 修复前这里是 false。
- 一键修复端到端（一次性路径 `D:\_wbacl_test`）：`Repair-BwBaseDir` 返回 True，耗时 1.6 秒；
  之后**非提权**可写文件；ACL 只有 `Authenticated Users:(OI)(CI)(M)` 一条显式项，
  子目录继承到同一条；**递归删除成功**（即"删掉文件夹 = 完全回退"这条回退路径成立）。
- 首次运行向导无人值守跑通（用同名函数覆盖 `Read-Host` 把答案排队喂进去，测的仍是产品代码）：
  输入 `D:\_wbtest_ui` → 弹出修复询问 → 选 `[1]` → 建库并下载（必应 1 张 / 聚焦 6 张）→
  `config.json` 落到 `D:\_wbtest_ui\必应|聚焦`；`Read-Host` 提示序列正好 4 条，与预期一致。
- 列表渲染四个分支逐一验证：能用的盘、当前写不进去的盘（带说明**且仍在编号内**）、
  系统盘（标"不推荐"、排最后）、网络盘。
- `Repair-BwBaseDir` 入参校验：裸盘根 `D:\`、UNC 路径、相对路径、空串 —— 一律拒绝**且不触发提权**。

## v1.2.2 — 2026-09-18

**修复：向导会推荐一个写不进去的盘**
- 现象：首次运行向导里选 `[2] D:\`，程序只回一句"这个位置写不进去"就退回默认位置。
  列表里 D 盘和 E/F 并排列着，看不出差别，用户无从判断。
- 根因：挑候选盘时只看"盘在不在"，没看"**能不能在这个盘根目录里建文件夹**"。
  实测本机三个盘的根目录权限并不一样：

  | 盘 | 根目录 ACE（普通用户） | 实测 `Directory.CreateDirectory` |
  |---|---|---|
  | `D:\` | 只有 `BUILTIN\Users: ReadAndExecute` | **访问被拒绝** |
  | `E:\` | `Authenticated Users: Modify`（仅此文件夹） | OK |
  | `F:\` | 同上 | OK |

  D 盘根目录缺"修改"权限，普通用户根本建不出 `D:\微软壁纸助手` —— 跟选哪个子目录无关。
- 新增 `Test-BwRootWritable`：真建一个临时目录再删掉，探测盘根可写性
  （进程内缓存，每个盘只探一次）。
- `Get-BwDriveChoices` 每项多带一个 `Writable`；向导**分两组展示**：能写的正常编号
  （非系统盘优先、按可用空间降序），不能写的单列并写明原因，且**不参与编号** ——
  用户不会再选到一个注定失败的盘。
  ⚠️ **这条做法 v1.2.3 已推翻**：把盘从列表里剔除会让用户以为"程序不支持我这个盘"，
  实际那只是本机权限设置。v1.2.3 改为全部列出、一个不筛，并给一键修复。见下面 v1.2.3 条目。
- `Get-BwPickRoot` 把"可写"提升为硬条件：非系统盘且可写 → 非系统盘 → 任意本地盘 →
  可写网络盘 → 任意网络盘，避免自动挑到写不进去的盘。

**修复：`New-Item -LiteralPath` 在某些环境下把探测搞成假阴性**
- 探测原本用 `New-Item -LiteralPath`，但本机 PowerShell 的 `New-Item` 参数集被裁过
  （`Get-Command New-Item` 的参数里**没有 `LiteralPath`**），传它直接抛
  "找不到与参数名称匹配的参数" —— 那是**环境问题不是权限问题**，会让所有盘都被判成不可写。
- 改用 .NET API（`Directory.CreateDirectory` / `Directory.Delete`）建删探测目录，
  不依赖任何 cmdlet 参数集。

**测试**
- 探测对照：`D:\` / `E:\` / `F:\` 同一段代码 → `False` / `True` / `True`，与 ACL 分析一致；
  `D:\` 的 .NET 异常原文是"对路径 ... 的访问被拒绝"。
- 向导渲染实测：`[1] E:`（推荐）→ `[2] F:` → `[3] C:`（系统盘，不推荐）→
  `D:\ 用不了: 根目录没给普通用户新建文件夹的权限`。
- `Get-BwPickRoot` = `E:\`；`Get-BwDefaults.bing` = `E:\微软壁纸助手\必应`。
- 临时探测目录在四个盘上全部清干净（残留 0 个）。

## v1.2.1 — 2026-09-18

**修复：默认保存位置永远落在 F 盘**
- `Get-BwPickRoot` 里用 `[psobject]@{ ... }` 构造磁盘条目，PowerShell 实际生成的是
  **Hashtable**，`Sort-Object Free -Descending` 取不到 `.Free` 属性，**排序静默失效**
  （实测退化成 `F: → E: → D:`）。"挑可用空间最大的非系统盘"因此恒等于挑 F 盘，
  哪怕别的盘可用空间是它的两倍。v1.2.0 声称的"自动挑最大非系统盘"实际从未生效。
- 改成 `[PSCustomObject]@{ ... }`。本机实测：修复前 `Get-BwPickRoot` = `F:\`，
  修复后 = `E:\`（256.7 GB，四个盘里最大）。

**首次运行向导：把可选的盘列出来**
- 以前只给一行"建议保存到 `X:\微软壁纸助手`"，想换只能自己敲完整路径。
- 现在按可用空间从大到小列出所有盘（非系统盘在前，C 盘标注"系统盘，不推荐"），
  输入序号即可选，回车用推荐值，也仍然支持直接粘贴完整路径。
- 新增 `Get-BwDriveChoices`：列出可用于保存的盘（本地固定盘 + 网络盘）。条目同样用
  `[PSCustomObject]` 构造，避免重蹈上述排序坑。

**菜单按键**
- 新增 `Normalize-BwKey`：按键统一做「去空格 + 全角转半角 + 转小写」，
  中文输入法把 `o` 打成全角 `ｏ` 时不再"按了没反应"。
- 修掉 `switch` 里 `'a'` / `'A'` 各写一条导致**开关被切两次**（等于没切）的问题。
  依据：PowerShell 的 `switch` 对字符串大小写不敏感，且匹配到的子句会**全部执行**。
- 退出键改为 `[Q]`（`0` 和 `o` 同样有效）；输入非菜单项时给明确提示，不再静默重画。
- 打开「开机自动换壁纸」前先清掉残留的 `daemon.stop`，否则刚拉起的后台会立刻自己退掉。

**测试（均为实测）**
- `Get-BwPickRoot` 前后对比：`F:\` → `E:\`。
- `Get-BwDriveChoices` 顺序：`E(256.7) → D(152.2) → F(143.1) → C(79.5, 系统盘)`。
- 排序失效的三种写法对照：`[psobject]@{}` → `F,E,D`（错）；裸 `@{}` → `F,E,D`（错）；
  `[PSCustomObject]@{}` → `E,D,F`（对）。
- 打包后 `pefile` 读 PE 头：`Subsystem=2 (WINDOWS_GUI)`；`CArchiveReader` 解出 4 个内嵌文件，
  与源文件 MD5 全等（`core.ps1` 29549 / `menu.ps1` 15882 / `使用说明.txt` / `.ico`）。
- 隔离目录跑 `--once`：返回码 0，`.version` = 1.2.1，成功下载并切换到当日必应图。

## v1.2.0 — 2026-09-18

**绿色单文件版（本次主要变化）**
- 新增 `launcher.py` + `build-exe.py`：把一个启动器打成 **单个 GUI 子系统 exe**（约 7.2 MB）。
  双击即用，不安装、不写注册表、不需要管理员权限；删掉文件夹就是彻底卸载。
- 首次运行会在 exe 同级自动建「微软壁纸助手数据」目录，把内嵌的 `core.ps1` / `menu.ps1`
  释放进去，并记版本号（换新版 exe 会按版本号自动重放）。
- 「开机自动换壁纸」改成绿色做法：菜单按 `[A]` 往 **Windows 启动文件夹**放一个快捷方式
  （指向 `exe --daemon`），不需要管理员，也不注册计划任务。再按一次 `[A]` 即关闭，
  并通过 `daemon.stop` 通知后台进程收工。
- 后台常驻用命名互斥体保证单实例；`daemon.stop` 分片轮询，最长 15 秒内退出。

**无黑窗（更彻底）**
- 根因没变：`powershell.exe` 的 PE 子系统是 CONSOLE，Windows 先建控制台再执行隐藏，必然闪窗。
- v1.1.0 用 `wscript.exe`（GUI 子系统）绕开；v1.2.0 进一步把启动器本身做成 GUI 子系统 exe，
  **需要菜单时才自己 `AllocConsole()`**，后台模式一个控制台都不创建。
  顺带摆脱了对 VBScript 的依赖（微软已宣布逐步弃用 VBScript）。
- 实测：`pefile` 读 PE 头，本 exe `Subsystem=2 (WINDOWS_GUI)`；后台模式下
  `GetConsoleWindow()` 始终为 0；菜单模式下 `AllocConsole()` 成功后 PowerShell 子进程正常继承控制台。

**保存位置不再写死（分享给别人的前提）**
- 旧版默认写死 `D:\Bing壁纸` / `D:\聚焦壁纸`，机器上没有 D 盘就直接失败。
- 现在按顺序自动挑：本地固定磁盘里可用空间最大的**非系统盘** → 只有 C 盘就用 C 盘 →
  都读不到才退到 `%USERPROFILE%\Pictures`。首次运行定一次并写进 `config.json`，之后不再漂。
- 磁盘枚举结果在单次运行内缓存（`Get-BwConfig` 调用很频繁，不能每次都去枚举磁盘）。
- 设置项不再拒绝 C 盘；改为「真写一个临时文件再删」来判断目录是否可写，
  并要求输入完整路径（`D:\...` 或 `\\server\share`）。

**菜单**
- 新增 `[3] 补下错过的必应`：手动触发补漏，前后各报一次库里的张数。
- 新增 `[9] 打开保存壁纸的文件夹`（`explorer.exe`）。
- 新增 `[A] 开机自动换壁纸 开/关`，顶部状态行显示当前是开还是关。
- 顶部状态行增加必应库张数与保存位置。
- 首次运行向导：不再要求手填，直接给出自动挑好的位置，回车即接受；
  路径不合法或不可写时自动退回默认位置并说明原因。
- `[8] 设置` 里输入 `o` 可直接打开对应文件夹。

**修复**
- `Save-BwFile` 现在会先创建目标目录再写文件，避免首次运行 / 换盘后 `OutFile` 直接抛异常。
- `Invoke-BwCycle` 开头先 `Ensure-BwDirs`，保证补漏和下载有地方落盘。

**测试（均为实测，不是"应该能行"）**
- exe 子系统类型：`Subsystem=2 (WINDOWS_GUI)`，与 notepad.exe 同类。
- 静默跑一轮：`exe --once` 退出码 0，`state.json` 时间戳确实被刷新（1.8 秒 / 次，无事可做时）。
- 后台常驻：`--daemon` 起 2 个进程（PyInstaller 引导器 + 本体）；再起一个立刻退出（互斥体生效）；
  `--stop` 后进程清零。
- 菜单模式：`exe` 无参启动后进程树为 `exe → exe → powershell.exe`，确认菜单被拉起。
- 控制台链路：GUI 子系统下 `AllocConsole()` 成功、标准句柄重开、Python 中文输出正常、
  PowerShell 子进程继承控制台并可 `Read-Host`。
- 启动文件夹：非管理员身份创建/读取/删除 `.lnk` 全部成功，参数 `--daemon`、图标写入正确。

**已知限制**
- 如果同时开着旧版计划任务（`MSWallpaperDaily`）和新的后台常驻，两边会轮流抢壁纸。
  二选一，别同时开。

## v1.1.0 — 2026-09-18

**菜单**
- 三层菜单压成单层：主菜单 → 子菜单 → 归档，共 8 项全部直达，日常操作从按 2~3 次键降到 1 次。
- 补上「随机回忆」入口（`Invoke-BwRandom` 早已实现，但旧菜单没有入口）。
- 两个子菜单里的设置项合并成一处「[7] 设置」，回车即跳过，一次问完。
- 「浏览壁纸库」合并必应 + 聚焦两个库，按时间倒序取最近 40 张并标注来源。
- 顶部状态行增加「下次自动换图」时间与当前壁纸文件名。

**无黑窗**
- 根因：计划任务直接启动 `powershell.exe`（PE 子系统 = CONSOLE），Windows 先建控制台窗口再执行隐藏，必然闪窗。
- 修复：新增 `run-hidden.vbs`，计划任务改为经 `wscript.exe`（PE 子系统 = GUI）拉起，再以窗口样式 0 启动 powershell。
- 新增 `install-task.ps1`：一键（重新）注册计划任务，含回退说明。

**补漏（新功能）**
- 几天没开机时，自动把错过的每一天必应壁纸补下载到必应库，一张不少。
- 缺口 ≤ 7 天走必应官方接口，更早的走 niumoo 归档源；只下载不切壁纸，幂等。

**修复**
- `Get-BwDisplayDate` 一直解析失败：官方 `fullstartdate` 是 12 位 `yyyyMMddHHmm`（无秒），
  旧代码按 14 位 `yyyyMMddHHmmss` 解析必然抛异常，掉进兜底分支返回"今天"。
  现在改用官方 `enddate` 字段（等价于原来的 +8 小时约定，但不再依赖易错的时间解析）。
- 「今日首次换必应」不再要求接口日期等于今天。必应下午 16:00 才发新图，
  旧逻辑在此窗口开机时会整天既不下载也不切换，与"不能错过任何一张"直接冲突。
- 队列补货张数默认值 20 → 6（`spotlight_per_cycle`）。

**测试**
- 菜单 8 个分支逐个跑通，无报错。
- 补漏：7 天内缺口（官方接口）与超过 7 天缺口（归档源）两条路径实测通过。
- 轮换：试运行验证首轮记账切换、同日第二次 tick 静默不重复切。
- 计划任务注册逻辑在非特权测试任务中验证通过（含参数名纠错）。
- 无窗口启动以 PE 子系统类型作为依据（powershell=CONSOLE / wscript=GUI）。

## v1.0.0 — 2026-09-17

首个版本（内部使用，未公开）。

- 双图源：必应每日一图 + Windows 聚焦。
- 三条自动节奏：每天首次换必应、重启换聚焦、每 30 分钟换聚焦。
- 聚焦队列洗牌机制，库内不重复；队列发完自动补货重洗。
- 往期必应归档批量下载（2021-02 至今 4K）。
- 三层交互菜单、状态总览、运行日志。
